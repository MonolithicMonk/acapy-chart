apiVersion: v1
kind: Service
metadata:
  name: {{ include "acapy-chart.fullname" . }}
  labels:
    {{- include "acapy-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acapy-chart.selectorLabels" . | nindent 4 }}
