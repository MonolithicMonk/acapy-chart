apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acapy-chart.fullname" . }}-test-connection"
  labels:
    {{- include "acapy-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acapy-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
